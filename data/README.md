# data/ — 数据目录

> ⚠️ **新手可忽略此目录**。课程要求必须使用 **MySQL 数据库**存取数据，这里只是放一些辅助文件。

## 用途

| 目录 | 说明 |
|------|------|
| `sample/` | 配置示例、导出备份（可选） |
| `runtime/` | 本地运行时临时文件（已 gitignore） |

## 主数据存储

- 数据库：MySQL，脚本见 `sql/`
- 访问方式：`dao` 层 JDBC
