# 数据目录说明

> **注意**：课程要求必须使用 **数据库** 存取数据，本目录**不是**主数据存储。

## 用途

| 目录/文件 | 说明 |
|-----------|------|
| `sample/` | 配置示例、导出备份（可选） |
| `runtime/` | 本地运行时临时文件（已 gitignore） |

## 主数据存储

- 数据库：MySQL，脚本见项目根目录 `sql/`
- 访问方式：`dao` 层 JDBC
