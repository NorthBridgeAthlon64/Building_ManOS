---
description: Java 分层架构与编码约定
globs: "**/*.java"
alwaysApply: false
---

# Java 分层约定

## 禁止

- `cli` 直接写 SQL 或 `new XxxDao()` 以外的数据库操作
- `dao` 写业务判断（如「能否删除楼盘」在 service）
- 引入 GUI/Web 依赖

## 新建类放置

| 类型 | 包 |
|------|-----|
| 菜单/控制台 | `cli` |
| 业务逻辑 | `service` |
| JDBC | `dao` |
| 表实体 | `model` |
| 折扣算法 | `discount` |

## 编码习惯

- 使用 `PreparedStatement`，参数 `?` 绑定
- Service 抛业务异常；DAO 抛数据访问异常或返回 Optional/null
- 购买流程：校验在售 → 算折扣 → 更新 house + 插入 sale_record
- 总价：`area * unitPrice`，折扣用 `DiscountStrategy#apply`

## 作者

新建类时 `@author` 填实际编写者（见团队分工表），勿统一写「技术组」。
